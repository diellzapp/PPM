<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:gravity="center_horizontal"
    android:layout_marginTop="20dp">

    <!-- Imazhi i ofertës -->
    <ImageView
        android:id="@+id/detailImage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:adjustViewBounds="true"
        android:scaleType="fitCenter"
        android:layout_marginBottom="20dp"
        android:contentDescription="Oferta Image" />

    <!-- ScrollView për përshkrimin e detajuar të ofertës -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginBottom="20dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">


            <TextView
                android:id="@+id/detailDescription"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Description: Kjo është oferta më e mirë për ju."
                android:textSize="16sp"
                android:lineSpacingExtra="6sp"
                android:textColor="#555555" />
        </LinearLayout>
    </ScrollView>

    <!-- Butoni për të kthyer në ofertat -->
    <Button
        android:id="@+id/buttonBack"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Back to offers"
        android:textColor="#ffffff"
        android:background="@drawable/button_background"
        android:textSize="18sp"
        android:padding="12dp"
        android:layout_gravity="center" />
</LinearLayout>
